%table.table.table-hover.table-striped.table-bordered#user-solutions
  %tbody
    = render :partial => 'users/solution', :collection =>@solutions

= will_paginate @solutions, :params =>{:action => 'solutions'}, :noajax => true
