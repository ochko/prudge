%ul
  %li!= link_to 'Шинэ хичээл нэмэх', new_lesson_path
  - if @lesson && can?(:modify, @lesson)
    %li!= link_to 'Засварлах', edit_lesson_path(@lesson)
    %li!= link_to 'Устгах', @lesson, :confirm => 'Are you sure?', :method => :delete

