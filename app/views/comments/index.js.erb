$("#comments").append("<%= escape_javascript(render(:partial => 'comments/comment', :collection => @comments)) %>");
$("#flow_pagination").replaceWith("<%= escape_javascript(paginate(@comments, :params =>{:controller => '/comments' })) %>");
