defaults: &defaults
  git: "/usr/local/bin/git"
  diff: "/usr/bin/diff -bZu"
  runner: ":root/judge/sandbox/safeexec"

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
